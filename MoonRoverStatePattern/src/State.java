
public interface State {

	void pressRightPedal(Context context, int numOfTimesPressed, int numOfSecondsPressed);

	void pressLeftPedal(Context context, int numOfTimesPressed, int numOfSecondsPressed);

}